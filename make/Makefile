#
# Makefile --Build rules for the make directory of makeshift.
#
# Contents:
#
# Remarks:
# The "toc" target cannot be run until things have been installed in DEV_HOME.
#
MK_SRC = coverage.mk deploy.mk makeshift.mk doxygen.mk ld.mk \
    library.mk package.mk recursive-targets.mk repository.mk \
    std-directories.mk valid.mk version.mk

include makeshift.mk

install:	install-mk
uninstall:	uninstall-mk
