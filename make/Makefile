#
# Makefile --Build rules for the make directory of makeshift.
#
MK_SRC = component.mk coverage.mk deploy.mk doxygen.mk ld.mk \
    library.mk makeshift.mk package.mk recursive-targets.mk \
    repository.mk std-directories.mk valid.mk

include makeshift.mk

install:	install-mk
uninstall:	uninstall-mk
